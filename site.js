(()=>{const root=document.documentElement;const home=document.querySelector('.home-hero');const logo=document.querySelector('.hero-logo');const progress=document.querySelector('.hero-progress i');if(home&&logo){const tick=()=>{const r=home.getBoundingClientRect();const p=Math.max(0,Math.min(1,-r.top/(home.offsetHeight-innerHeight)));root.style.setProperty('--progress',`${p*100}%`);root.style.setProperty('--lx',`${Math.sin(p*Math.PI*2)*18}px`);root.style.setProperty('--ly',`${(p-.5)*120}px`);root.style.setProperty('--lr',`${(p-.5)*24}deg`);root.style.setProperty('--ls',`${1+Math.sin(p*Math.PI)*.13}`);root.style.setProperty('--ring',`${p*80}deg`);root.style.setProperty('--ringScale',`${1+p*.18}`);if(progress)progress.style.width=`${p*100}%`;requestAnimationFrame(tick)};tick()}const product=document.querySelector('.product-scene');const obj=document.querySelector('.product-object');if(product&&obj){const tick=()=>{const r=product.getBoundingClientRect();const p=Math.max(0,Math.min(1,-r.top/(product.offsetHeight-innerHeight)));root.style.setProperty('--px',`${Math.sin(p*Math.PI*2)*50}px`);root.style.setProperty('--py',`${(p-.5)*100}px`);root.style.setProperty('--pr',`${p*360}deg`);root.style.setProperty('--ps',`${1+Math.sin(p*Math.PI)*.2}`);requestAnimationFrame(tick)};tick()}})();
